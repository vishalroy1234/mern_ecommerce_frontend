import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile Page" description="A page for user to sign in!">
      <h1>profile page</h1>
    </Base>
  );
};

export default Profile;
